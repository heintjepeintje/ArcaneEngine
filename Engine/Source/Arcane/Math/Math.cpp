#include "Math.hpp"

#include <cmath>

namespace Arcane {

	float Sin(float angle) {
		return sinf(angle);
	}

	float Cos(float angle) {
		return cosf(angle);
	}

	float Tan(float angle) {
		return tanf(angle);
	}

	float Sqrt(float value) {
		return sqrtf(value);
	}

}